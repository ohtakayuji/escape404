{
  "name": "escape404",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ESCAPE 404 - THE LAST DEVELOPER. First-person 3D puzzle escape game.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^26.4.0",
    "@types/three": "^0.185.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  }
}
